<template>
  <section class="relative gradient-hero py-16 md:py-24 overflow-hidden">
    <!-- Decorative elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-0 right-0 w-72 h-72 rounded-full bg-white/5 blur-3xl"></div>
      <div class="absolute bottom-0 left-0 w-64 h-64 rounded-full bg-white/5 blur-3xl"></div>
      <div class="absolute top-1/3 left-[20%] w-3 h-3 rounded-full bg-white/20 animate-pulse-soft"></div>
      <div class="absolute top-2/3 right-1/4 w-2 h-2 rounded-full bg-white/15 animate-pulse-soft" style="animation-delay: 0.8s;"></div>
    </div>

    <div class="container-custom relative z-10">
      <div class="max-w-2xl mx-auto text-center">
        <!-- Icon -->
        <div class="inline-flex items-center justify-center w-14 h-14 rounded-full bg-white/15 backdrop-blur-sm mb-6">
          <svg class="h-7 w-7 text-white" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
          </svg>
        </div>

        <!-- Heading -->
        <h2 class="text-3xl md:text-4xl font-heading font-bold text-white mb-4" data-i18n="newsletter.title">
          Stay in the Loop
        </h2>
        <p class="text-white/80 text-base md:text-lg mb-8 leading-relaxed" data-i18n="newsletter.subtitle">
          Be the first to know about new arrivals, exclusive offers, and style inspiration delivered straight to your inbox.
        </p>

        <!-- Form -->
        <form v-if="!subscribed" class="flex flex-col sm:flex-row gap-3 max-w-lg mx-auto" @submit.prevent="handleSubmit">
          <label for="newsletter-email" class="sr-only">Email address</label>
          <input
            v-model="email"
            type="email"
            id="newsletter-email"
            name="email"
            placeholder="Enter your email address"
            data-i18n-placeholder="newsletter.placeholder"
            required
            autocomplete="email"
            class="flex-1 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm px-5 py-3.5 text-white placeholder-white/50 transition-colors focus:border-white/40 focus:outline-none focus:ring-2 focus:ring-white/20 text-sm"
          />
          <button
            type="submit"
            class="inline-flex items-center justify-center gap-2 rounded-lg bg-white px-6 py-3.5 font-heading font-semibold text-sm tracking-wide text-primary-600 shadow-lg transition-all duration-200 hover:shadow-xl hover:scale-105 focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-primary-600 cursor-pointer shrink-0"
          >
            <span data-i18n="newsletter.subscribe">Subscribe</span>
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
            </svg>
          </button>
        </form>

        <!-- Success message -->
        <div v-else class="mt-4 text-sm text-white/90 bg-white/10 backdrop-blur-sm rounded-lg px-4 py-3">
          <div class="flex items-center justify-center gap-2">
            <svg class="h-5 w-5 text-emerald-300" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span>Thanks for subscribing! Check your inbox for a welcome surprise.</span>
          </div>
        </div>

        <!-- Disclaimer -->
        <p class="text-white/50 text-xs mt-4">
          No spam, unsubscribe anytime. We respect your privacy.
        </p>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
const email = ref('');
const subscribed = ref(false);

function handleSubmit() {
  if (email.value.trim()) {
    subscribed.value = true;
  }
}
</script>
