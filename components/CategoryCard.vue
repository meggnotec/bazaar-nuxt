<template>
  <NuxtLink
    :to="`/categories/${category.slug}`"
    class="group relative block overflow-hidden rounded-2xl aspect-[4/3] shadow-md hover:shadow-xl hover:shadow-primary-500/10 transition-all duration-300"
  >
    <!-- Background -->
    <img
      v-if="category.image"
      :src="category.image"
      :alt="category.name"
      width="800"
      height="600"
      loading="lazy"
      class="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
    />
    <div
      v-else
      class="absolute inset-0 transition-transform duration-500 group-hover:scale-110"
      :style="{ backgroundColor: category.placeholderColor }"
    >
      <div class="w-full h-full flex items-center justify-center">
        <span class="text-white/30 font-heading text-lg">{{ category.name }}</span>
      </div>
    </div>

    <!-- Gradient overlay -->
    <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent"></div>

    <!-- Content -->
    <div class="absolute bottom-0 left-0 right-0 p-6">
      <h3 class="font-heading font-bold text-xl text-white mb-1 group-hover:text-primary-300 transition-colors">
        {{ category.name }}
      </h3>
      <p class="text-white/70 text-sm mb-2">{{ category.description }}</p>
      <div class="flex items-center justify-between">
        <span class="text-xs font-medium text-white/60">{{ category.productCount }} Products</span>
        <span class="inline-flex items-center gap-1 text-xs font-semibold text-white group-hover:text-primary-300 transition-colors">
          Shop Now
          <svg class="h-3.5 w-3.5 transition-transform duration-200 group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
          </svg>
        </span>
      </div>
    </div>

    <!-- Product count badge -->
    <div class="absolute top-4 end-4 bg-white/90 dark:bg-surface-800/90 backdrop-blur-sm rounded-full px-3 py-1">
      <span class="text-xs font-bold text-surface-900 dark:text-white">{{ category.productCount }}</span>
    </div>
  </NuxtLink>
</template>

<script setup lang="ts">
import type { Category } from '~/types';

defineProps<{
  category: Category;
}>();
</script>
